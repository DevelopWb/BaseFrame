<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="3dp"
    >

    <TextView
        android:id="@+id/tabitem_text"
        style="@style/tab_textview"
        android:gravity="center"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:singleLine="true"
        android:text="上报"
        android:textColor="@color/text_main_tab"
        android:textSize="16sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:layout_width="30dp"
        android:layout_height="15dp"
        android:layout_marginTop="5dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/sp_filled_white_top_circle"
        app:layout_constraintRight_toRightOf="@+id/tabitem_text"
        app:layout_constraintLeft_toLeftOf="@+id/tabitem_text"
        app:layout_constraintTop_toBottomOf="@+id/tabitem_text" />
</android.support.constraint.ConstraintLayout>